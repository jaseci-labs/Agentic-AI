name: Deploy JAC-GPT Fullstack with jac-scale

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy-jac-gpt-fullstack:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/jac-gpt-') || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      # Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Configure AWS credentials using OIDC
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4.2.0
        with:
          aws-region: us-east-2
          role-to-assume: arn:aws:iam::776241927220:role/GitHubActionsJaseciDeployRole
          role-session-name: GitHubActions-JacGPT
          audience: sts.amazonaws.com

      # Update kubeconfig to connect to EKS cluster
      - name: Update kubeconfig for EKS
        run: |
          aws eks update-kubeconfig --region us-east-2 --name jaseci-cluster
          kubectl config current-context

      # Set up Python
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # jaclang ships as a self-contained `jac` binary. Under jaseci#7084,
      # `jac start --scale` ships this same binary to the pods over a
      # ReadWriteMany bundle PVC -- no source clone, no code-server, no
      # --experimental. We use the rolling `dev` prerelease (rebuilt from main),
      # NOT latest stable: v0.32.0 stable (2026-07-13) predates jaseci/jac#7420
      # (merged 2026-07-14), the fix for issue #7412 -- "client bundler omits the
      # __jacJsx factory", which blanks the app ("__jacJsx is not defined") on
      # rich/error renders. The dev binary carries #7420; jac.toml's bare [dev]
      # table tells the pods to run this same dev binary. Matches jacBuilder's
      # deploy-dev.yml. Swap `dev` for a pinned release tag once stable ships #7420.
      - name: Install the jac dev binary (carries the #7420 __jacJsx client fix)
        run: |
          mkdir -p "$HOME/.jacbin"
          curl -fsSL "https://github.com/jaseci-labs/jaseci/releases/download/dev/jac-dev-linux-x86_64" -o "$HOME/.jacbin/jac"
          chmod +x "$HOME/.jacbin/jac"
          echo "$HOME/.jacbin" >> "$GITHUB_PATH"
          export PATH="$HOME/.jacbin:$PATH"
          jac --version
          jac start --help | grep -q -- "--scale"

      # Install app deps. The .jab seal resolves the app's Python imports from
      # the GLOBAL jac site (the client compile pulls in server modules like
      # rag_engine -> langchain/sentence-transformers), so the heavy deps must be
      # installed --global too, or the seal skips those modules ("Seal incomplete").
      - name: Install app dependencies
        run: |
          jac install
          jac install --global \
            python-dotenv requests numpy \
            langchain-openai langchain-community langchain-chroma \
            langchain-text-splitters langchain-core pypdf \
            sentence-transformers faiss-cpu
        working-directory: jac-gpt-fullstack

      # Deploy using jac-scale (secrets from env via [scale.secrets]). The binary
      # is shipped to pods over the RWX bundle PVC (bundle_storage_class). Under
      # #7084, `jac start --scale` ships the binary, applies the manifest (~2min),
      # then HANGS on non-daemon threads (litellm, k8s watch loops). timeout 1800
      # caps the hang; exit 124 is EXPECTED and treated as success. The
      # "Ready: downloading" marker is proof the manifest was applied (any prep
      # failure raises non-124, surfaced below). Mirrors jacBuilder deploy-dev.yml.
      - name: Deploy JAC-GPT Fullstack with jac-scale
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          set +e
          timeout 1800 jac start main.jac --scale 2>&1 | tee /tmp/jacstart.log
          EXIT_CODE=${PIPESTATUS[0]}
          set -e
          if [ "$EXIT_CODE" -ne 0 ] && [ "$EXIT_CODE" -ne 124 ]; then
            echo "jac start failed with exit code $EXIT_CODE"
            exit $EXIT_CODE
          fi
          if [ "$EXIT_CODE" -eq 124 ]; then
            echo "jac start hit the 1800s cap (expected: it hangs on non-daemon threads once the deploy is applied)"
          fi
          if ! grep -q "Ready: downloading" /tmp/jacstart.log; then
            echo "ERROR: jac start never reported the runtime Ready; NO manifest was applied. Failing loudly."
            exit 1
          fi
          echo "Deployment spec applied by jac-scale."
        working-directory: jac-gpt-fullstack

      # Get deployment status
      - name: Verify deployment
        run: |
          echo "Checking deployment status..."
          kubectl get deployments -n jac-gpt-test
          kubectl get services -n jac-gpt-test
          kubectl get pods -n jac-gpt-test

      # Output deployment summary
      - name: Deployment Summary
        if: always()
        run: |
          echo "## JAC-GPT Fullstack Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "### Configuration" >> $GITHUB_STEP_SUMMARY
          echo "- **App Name**: jac-gpt-test" >> $GITHUB_STEP_SUMMARY
          echo "- **Namespace**: jac-gpt-test" >> $GITHUB_STEP_SUMMARY
          echo "- **Cluster**: jaseci-cluster" >> $GITHUB_STEP_SUMMARY
          echo "- **Region**: us-east-2" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered By**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          if [[ "${{ github.event_name }}" == "release" ]]; then
            echo "- **Release Tag**: ${{ github.event.release.tag_name }}" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Deployment Status" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          kubectl get all -n jac-gpt-test 2>&1 >> $GITHUB_STEP_SUMMARY || echo "Unable to fetch deployment status" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
